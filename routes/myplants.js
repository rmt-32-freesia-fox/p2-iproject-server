const router = require('express').Router()
const Controller = require('../controllers')


router.post('/', )
router.get('/', )
router.delete('/:id', )
router.put('/',)
router.get('/payment', )


module.exports = router